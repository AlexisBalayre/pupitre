/** Consecutive normalized source lines that must all match for a block to count as duplicated. */
export const DUPLICATION_WINDOW_LINES = 6;

/** Coverage reruns the full suite instrumented, so it gets the gate-command budget. */
export const COVERAGE_RUN_TIMEOUT_MS = 10 * 60 * 1000;
