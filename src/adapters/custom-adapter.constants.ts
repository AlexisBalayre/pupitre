/** Repo-relative location of the escape-hatch config (docs/06 "Custom adapters"). */
export const CUSTOM_ADAPTER_CONFIG_PATH = '.pupitre/adapter.yml';

/** Custom capability commands get the same budget as gate commands. */
export const CUSTOM_COMMAND_TIMEOUT_MS = 10 * 60 * 1000;
